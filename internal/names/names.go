// Package names provides interned string handles for Writst identifiers used as
// function names, method names, and parameter names throughout the ir package.
// Using [unique.Handle] avoids repeated string comparisons during evaluation
// and field lookup.
package names

import "znkr.io/writst/name"

var (
	Angle       = name.Make("angle")
	Arguments   = name.Make("arguments")
	Array       = name.Make("array")
	Assert      = name.Make("assert")
	At          = name.Make("at")
	Base        = name.Make("base")
	Block       = name.Make("block")
	Body        = name.Make("body")
	By          = name.Make("by")
	Bytes       = name.Make("bytes")
	Calc        = name.Make("calc")
	Children    = name.Make("children")
	Chunks      = name.Make("chunks")
	Content     = name.Make("content")
	Count       = name.Make("count")
	Decimal     = name.Make("decimal")
	Dedup       = name.Make("dedup")
	Default     = name.Make("default")
	Depth       = name.Make("depth")
	Description = name.Make("description")
	Dest        = name.Make("dest")
	Digits      = name.Make("digits")
	Document    = name.Make("document")
	Emoji       = name.Make("emoji")
	Emph        = name.Make("emph")
	Endian      = name.Make("endian")
	Enum        = name.Make("enum")
	Enumerate   = name.Make("enumerate")
	Eq          = name.Make("eq")
	Even        = name.Make("even")
	Exact       = name.Make("exact")
	Filter      = name.Make("filter")
	First       = name.Make("first")
	Float       = name.Make("float")
	Fold        = name.Make("fold")
	Fraction    = name.Make("fraction")
	FromBytes   = name.Make("from-bytes")
	Function    = name.Make("function")
	Heading     = name.Make("heading")
	Inf         = name.Make("inf")
	Init        = name.Make("init")
	Insert      = name.Make("insert")
	Int         = name.Make("int")
	Intersperse = name.Make("intersperse")
	IsInfinite  = name.Make("is-infinite")
	IsNan       = name.Make("is-nan")
	Item        = name.Make("item")
	Join        = name.Make("join")
	Key         = name.Make("key")
	Label       = name.Make("label")
	Lang        = name.Make("lang")
	Last        = name.Make("last")
	Len         = name.Make("len")
	Level       = name.Make("level")
	Linebreak   = name.Make("linebreak")
	Lines       = name.Make("lines")
	Link        = name.Make("link")
	List        = name.Make("list")
	Lorem       = name.Make("lorem")
	Map         = name.Make("map")
	Max         = name.Make("max")
	Message     = name.Make("message")
	Min         = name.Make("min")
	Named       = name.Make("named")
	Nan         = name.Make("nan")
	Ne          = name.Make("ne")
	Number      = name.Make("number")
	Odd         = name.Make("odd")
	Pairs       = name.Make("pairs")
	Par         = name.Make("par")
	Parbreak    = name.Make("parbreak")
	Pop         = name.Make("pop")
	Pos         = name.Make("pos")
	Position    = name.Make("position")
	Product     = name.Make("product")
	Push        = name.Make("push")
	Range       = name.Make("range")
	Ratio       = name.Make("ratio")
	Raw         = name.Make("raw")
	Reduce      = name.Make("reduce")
	Ref         = name.Make("ref")
	Relative    = name.Make("relative")
	Rem         = name.Make("rem")
	Remove      = name.Make("remove")
	Repeat      = name.Make("repeat")
	Repr        = name.Make("repr")
	Rev         = name.Make("rev")
	Round       = name.Make("round")
	Separator   = name.Make("separator")
	Signed      = name.Make("signed")
	Signum      = name.Make("signum")
	Sin         = name.Make("sin")
	Size        = name.Make("size")
	Slice       = name.Make("slice")
	Sorted      = name.Make("sorted")
	Split       = name.Make("split")
	Start       = name.Make("start")
	State       = name.Make("state")
	Step        = name.Make("step")
	Std         = name.Make("std")
	Str         = name.Make("str")
	Strong      = name.Make("strong")
	Sum         = name.Make("sum")
	Supplement  = name.Make("supplement")
	Sym         = name.Make("sym")
	Symbol      = name.Make("symbol")
	Table       = name.Make("table")
	Target      = name.Make("target")
	Term        = name.Make("term")
	Terms       = name.Make("terms")
	Text        = name.Make("text")
	Title       = name.Make("title")
	ToBytes     = name.Make("to-bytes")
	ToDict      = name.Make("to-dict")
	Trim        = name.Make("trim")
	Type        = name.Make("type")
	Update      = name.Make("update")
	Where       = name.Make("where")
	Windows     = name.Make("windows")
	With        = name.Make("with")
	Zip         = name.Make("zip")
)
