package names

import "unique"

var (
	Arguments   = unique.Make("arguments")
	Array       = unique.Make("array")
	At          = unique.Make("at")
	Base        = unique.Make("base")
	Block       = unique.Make("block")
	Body        = unique.Make("body")
	Bytes       = unique.Make("bytes")
	Children    = unique.Make("children")
	Chunks      = unique.Make("chunks")
	Content     = unique.Make("content")
	Count       = unique.Make("count")
	Decimal     = unique.Make("decimal")
	Default     = unique.Make("default")
	Depth       = unique.Make("depth")
	Description = unique.Make("description")
	Dest        = unique.Make("dest")
	Endian      = unique.Make("endian")
	Enumerate   = unique.Make("enumerate")
	Exact       = unique.Make("exact")
	First       = unique.Make("first")
	Float       = unique.Make("float")
	FromBytes   = unique.Make("from-bytes")
	Inf         = unique.Make("inf")
	Insert      = unique.Make("insert")
	Intersperse = unique.Make("intersperse")
	Int         = unique.Make("int")
	IsInfinite  = unique.Make("is-infinite")
	IsNan       = unique.Make("is-nan")
	Join        = unique.Make("join")
	Label       = unique.Make("label")
	Lang        = unique.Make("lang")
	Last        = unique.Make("last")
	Len         = unique.Make("len")
	Lines       = unique.Make("lines")
	Nan         = unique.Make("nan")
	Number      = unique.Make("number")
	Pop         = unique.Make("pop")
	Product     = unique.Make("product")
	Push        = unique.Make("push")
	Range       = unique.Make("range")
	Remove      = unique.Make("remove")
	Rev         = unique.Make("rev")
	Repr        = unique.Make("repr")
	Separator   = unique.Make("separator")
	Signed      = unique.Make("signed")
	Signum      = unique.Make("signum")
	Size        = unique.Make("size")
	Slice       = unique.Make("slice")
	Sorted      = unique.Make("sorted")
	Split       = unique.Make("split")
	Start       = unique.Make("start")
	Step        = unique.Make("step")
	Str         = unique.Make("str")
	Sum         = unique.Make("sum")
	Supplement  = unique.Make("supplement")
	Target      = unique.Make("target")
	Term        = unique.Make("term")
	Text        = unique.Make("text")
	ToBytes     = unique.Make("to-bytes")
	ToDict      = unique.Make("to-dict")
	Type        = unique.Make("type")
	Windows     = unique.Make("windows")
	Zip         = unique.Make("zip")
)
